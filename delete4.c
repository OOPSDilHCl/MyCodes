#include <stdio.h>
int main()
{
  int str[20];
  int a=1,b;
  b=649;
  printf("%d",2>1&&(b));
 if(1)
  {
  printf("%lu %lu %lu %lu %lu %lu %lu",sizeof(int),sizeof(unsigned long),sizeof(float),sizeof(str),sizeof(double),sizeof(long long),sizeof(short));
  } 
  return 0;
}